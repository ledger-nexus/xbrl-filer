# Shared Postgres with ledger-core (same database, companion pattern).
DATABASE_URL="postgresql://user:pass@host/db"
# Clerk (optional; when unset, write actions fail closed — same as recon)
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
# CLERK_SECRET_KEY=
